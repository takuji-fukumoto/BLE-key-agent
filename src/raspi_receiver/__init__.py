"""Raspberry Pi BLE key receiver package.

Provides a BLE GATT server library for receiving key events from Mac agent,
and application implementations (e.g., LCD display) built on top of the library.

Subpackages:
    lib: BLE GATT server library with callback-based key receiver API.
    apps: Application implementations using the library.
"""
