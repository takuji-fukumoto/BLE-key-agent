# BLE送受信ライブラリ化 実装計画（Mac/Pi）

- 作成日: 2026-03-01
- ブランチ: `feature/mac-input-ble-lib`
- 目的: `mac_agent` の BLE通信部と入力監視部に加え、`raspi_receiver/lib` のBLE受信部も、他リポジトリから利用しやすい再利用ライブラリとして整備する。
- 方針: 既存仕様（BLEプロトコル、再接続、pynputスレッド安全、受信側コールバック契約）を維持し、公開APIを明確化してアプリ層依存を排除する。

## タスク

- [x] [Phase1] 既存 `mac_agent` / `raspi_receiver/lib` の責務分解とライブラリ境界の確定
  - `ble_client.py` / `key_monitor.py` のUI依存・実行環境依存を洗い出す
  - `key_receiver.py` / `gatt_server.py` のアプリ依存・拡張ポイントを洗い出す
  - 互換維持が必要なインターフェースを確定する

- [x] [Phase2] 送信側/受信側ライブラリのパッケージ構造と公開API定義
  - エントリポイント（初期化、開始/停止、イベント購読）を設計する
  - 設定値（再接続、送信間隔、デバイス名等）の受け渡し方法を統一する

- [x] [Phase3] BLE通信機能の移設と互換レイヤ整備
  - `ble_client` 相当の機能をライブラリ層に移す
  - Write Without Response、再接続、ハートビート挙動を保持する

- [x] [Phase4] キー入力監視機能の移設とイベント橋渡し整理
  - `pynput` スレッド → `asyncio.Queue` 連携をライブラリ化する
  - 権限チェックとエラー通知インターフェースを整理する

- [x] [Phase5] 既存アプリ（CLI/外部GUI）を新ライブラリ利用へ切替
  - 現行エントリポイントの呼び出し先を置換する
  - UI層はロジックを持たない構成に寄せる

- [x] [Phase6] テスト更新とドキュメント整備
  - 既存テストをライブラリ境界に合わせて修正する
  - 利用手順・移行ガイドを `docs/` / `README.md` に反映する

- [ ] [Phase7] 動作確認（手動）と受け入れ確認
  - Mac↔Pi のキー送信E2Eを確認する
  - 接続断/再接続・監視ON/OFFを確認し結果をPRに記録する

- [ ] [Phase8] 受信側高レベルAPI（KeyReceiver）の利用容易性向上
  - 受信イベント購読（press/release/connect/disconnect）の登録方法を統一する
  - `start()` / `stop()` / `is_connected` / `stats` の契約を明文化する

- [ ] [Phase9] 受信側低レイヤAPI（GATTServer）の再利用インターフェース整理
  - `on_write` の契約とスレッド/イベントループ前提を明記する
  - 受信データ処理責務（デシリアライズ、heartbeat判定）の境界を整理する

- [ ] [Phase10] 受信側サンプル整備（他リポジトリ利用を想定）
  - 「文字入力バッファ」「Enter確定」など最小実用例を用意する
  - LCD依存なしのCLIサンプルを優先する

- [ ] [Phase11] 受信側テスト・ドキュメント更新
  - コールバック例外隔離、切断タイムアウト、heartbeat処理のテストを補強する
  - 利用ガイドに受信側APIの選択指針（高レベル/低レイヤ）を追記する

## Phase1 実行結果（責務分解と境界確定）

### 境界方針

- UI/アプリ層と通信/入力層を分離し、ライブラリ層はGUI実装やLCD描画に依存しない
- 共有プロトコルは `common.protocol` / `common.uuids` を唯一の真実源として維持
- BLE送信側は「入力取得」「送信」「統合制御」を分離し、呼び出し側が合成可能な形にする
- BLE受信側は `KeyReceiver`（高レベル）と `GATTServer`（低レイヤ）を明確に分離する

### モジュール責務マトリクス

| 層 | モジュール | 責務 | UI/ハード依存 |
|---|---|---|---|
| 共有 | `common.protocol` / `common.uuids` | Wire format / UUID定義 | なし |
| 送信-低レイヤ | `BleSender`（`mac_agent.ble_client`相当） | scan/connect/send/reconnect/heartbeat | なし |
| 送信-入力 | `KeyboardMonitor`（`mac_agent.key_monitor`相当） | pynput監視・Queue橋渡し | OS権限のみ |
| 送信-高レイヤ | `KeyBleAgent` | 入力→送信パイプライン統合 | なし |
| 受信-低レイヤ | `raspi_receiver.lib.GATTServer` | GATT起動/停止・write受信 | bless依存 |
| 受信-高レイヤ | `raspi_receiver.lib.KeyReceiver` | deserialize/callback/切断監視 | なし（LCD非依存） |
| アプリ | `mac_agent` UI / `raspi_receiver.apps.*` | 表示・設定・ユーザー操作 | あり |

### 互換維持対象（破壊変更しない契約）

- `KeyEvent` のJSONフォーマット（`t`, `v`, `p`, `mod`, `ts`）
- BLE送信の Write Without Response 優先
- 再接続バックオフ（指数、上限あり）
- heartbeat送受信と受信側タイムアウト切断判定
- 既存 `KeyReceiver` コールバック名（`on_key_press` など）

## Phase2 実行結果（公開APIと構造定義）

### 提供パッケージ構成（案）

```text
src/
  common/
    protocol.py
    uuids.py
  mac_agent/
    ble_client.py        # BleSender候補
    key_monitor.py       # KeyboardMonitor候補
    agent.py             # KeyBleAgent（新規予定）
    api_types.py         # AgentConfig / Callback型（新規予定）
  raspi_receiver/lib/
    gatt_server.py
    key_receiver.py
    types.py
```

### 送信側公開API（案）

```python
@dataclass
class AgentConfig:
    device_name: str = "RasPi-KeyAgent"
    reconnect_initial: float = 1.0
    reconnect_max: float = 60.0
    heartbeat_interval: float = 3.0
    min_send_interval: float = 0.005

class KeyboardMonitor:
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
    @property
    def is_running(self) -> bool: ...

class BleSender:
    async def scan(self, timeout: float = 5.0) -> list["BleDevice"]: ...
    async def connect(self, address: str) -> bool: ...
    async def disconnect(self) -> None: ...
    async def send_key(self, event: "KeyEvent") -> bool: ...
    @property
    def status(self): ...

class KeyBleAgent:
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
    @property
    def status(self): ...
```

### 受信側公開API（現行＋明文化）

```python
class KeyReceiver:
    on_key_press: Optional[Callable[["KeyEvent"], None]]
    on_key_release: Optional[Callable[["KeyEvent"], None]]
    on_connect: Optional[Callable[["ConnectionEvent"], None]]
    on_disconnect: Optional[Callable[["ConnectionEvent"], None]]

    async def start(self) -> None: ...
    async def stop(self) -> None: ...
    @property
    def is_connected(self) -> bool: ...
    @property
    def stats(self) -> "ReceiverStats": ...

class GATTServer:
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
    @property
    def is_running(self) -> bool: ...
```

### 設定値受け渡し統一方針

- 送信側は `AgentConfig` に集約し、コンストラクタ引数を最小化する
- 受信側は `device_name` とタイムアウト設定を明示引数化し、デフォルトを保持する
- 既存デフォルト値は変更せず、外部利用時のみ上書き可能にする

## Phase3 実行結果（BLE機能移設）

### 実装内容

- `BleClient` に再接続パラメータを追加し、ライブラリ利用時に外部設定可能化
  - `reconnect_initial_delay`
  - `reconnect_max_delay`
  - `reconnect_backoff_multiplier`
- `BleSender` クラスを追加し、送信用途の公開名を明示
  - 既存 `BleClient` は互換維持のため残置

### 変更ファイル

- `src/mac_agent/ble_client.py`

## Phase4 実行結果（入力監視移設）

### 実装内容

- `KeyboardMonitor` を追加（`KeyMonitor` ラッパー）
  - 外部Queue注入/内部Queue生成を選択可能
  - `next_event(timeout)` を提供
  - 既存 `KeyMonitor.check_accessibility()` を再利用
- `KeyBleAgent` を追加し、入力監視→BLE送信→heartbeat を統合する高レベルAPIを実装
  - `start()` / `stop()` / `status` / `scan()` / `connect()` を提供
  - `on_status_change` / `on_error` / `on_key_event` コールバック対応
- 送信側公開型 `AgentConfig` を追加

### テスト

- 新規: `tests/test_keyboard_monitor_wrapper.py`
- 新規: `tests/test_key_ble_agent.py`
- 回帰: `tests/test_ble_client.py`, `tests/test_key_monitor.py`

### 変更ファイル

- `src/mac_agent/api_types.py`
- `src/mac_agent/keyboard_monitor.py`
- `src/mac_agent/agent.py`
- `src/mac_agent/__init__.py`
- `tests/test_keyboard_monitor_wrapper.py`
- `tests/test_key_ble_agent.py`

## Phase5 実行結果（既存アプリ切替）

### 実装内容

- `src/mac_agent/main.py` を `KeyBleAgent` ベースへ移行
  - 旧 `MacAgent` 内の forward/heartbeat ループを削除
  - `KeyBleAgent.start()` / `stop()` を使用する薄いCLIオーケストレータへ変更
  - BLE接続処理は `agent.scan()` / `agent.connect()` に委譲
- Escキー押下で終了する挙動を `on_key_event` フックで維持
- macOS権限チェックを `KeyboardMonitor.check_accessibility()` に統一

### 変更ファイル

- `src/mac_agent/main.py`

## Phase6 実行結果（テスト更新とドキュメント整備）

### テスト更新

- `tests/test_ble_client.py`
  - 再接続設定パラメータの初期化テストを追加
  - disconnectテストで未await coroutine警告が出ないよう修正
- 既存＋新規テストを実行して回帰確認
  - `tests/test_ble_client.py`
  - `tests/test_key_monitor.py`
  - `tests/test_keyboard_monitor_wrapper.py`
  - `tests/test_key_ble_agent.py`
  - 結果: 45 passed

### ドキュメント更新

- `README.md` に送信側ライブラリ利用セクションを追加
  - `AgentConfig` / `KeyBleAgent` の利用例
  - 公開API一覧（`AgentConfig`, `KeyBleAgent`, `KeyboardMonitor`, `BleSender`）
- `src/mac_agent/__init__.py` のパッケージ説明を実態に合わせて更新

### 変更ファイル

- `tests/test_ble_client.py`
- `README.md`
- `src/mac_agent/__init__.py`

## 完了条件

- `mac_agent` コア機能（BLE通信・入力監視）を外部から利用可能なAPIとして提供できる
- `raspi_receiver/lib` 受信機能（KeyReceiver/GATTServer）を外部から利用可能なAPIとして提供できる
- 既存機能（再接続・ハートビート・低遅延送信）の挙動を維持できる
- 既存アプリが新ライブラリ経由で動作し、テストが通過する

## 実装ゲート

- 本計画はレビュー承認まで実装着手しない

## 改修完了後のライブラリ利用イメージ（公開API案）

### 想定ユースケース

- 他リポジトリ側は「初期化 → start() → stop()」のみで利用できる
- 接続状態やエラーはイベントコールバックで受け取る
- 既存CLI/外部GUIは薄いアダプタとして同じ公開APIを呼び出す

### 利用サンプル（案）

```python
import asyncio

from ble_key_input_client import AgentConfig, KeyBleAgent


async def main() -> None:
    agent = KeyBleAgent(
        AgentConfig(
            device_name="RasPi-KeyAgent",
            reconnect_initial=1.0,
            reconnect_max=60.0,
            heartbeat_interval=3.0,
        ),
        on_status_change=lambda status: print(f"status={status}"),
        on_error=lambda error: print(f"error={error}"),
    )
    await agent.start()
    try:
        await asyncio.Event().wait()
    finally:
        await agent.stop()


if __name__ == "__main__":
    asyncio.run(main())
```

### 公開API構成（案）

- `KeyBleAgent`
  - BLE送信 + キー監視を束ねる高レベルAPI
  - `start()` / `stop()` / `status` を提供
- `KeyboardMonitor`
  - pynput監視のみを提供（BLEなしで再利用可能）
- `BleSender`
  - BLE接続管理とキー送信のみを提供（入力源は任意）
- `AgentConfig`
  - 再接続設定・ハートビート・送信間隔・デバイス識別子などを集約

### 互換性ポリシー（案）

- 既存仕様の通信フォーマット（`common.protocol`）は維持
- Write Without Response優先、再接続バックオフ、ハートビート挙動を維持
- 既存 `mac_agent` からの移行は段階的に行い、薄い互換レイヤを提供する

## BLE受信側ライブラリ利用イメージ（公開API案）

### 想定ユースケース

- Pi側アプリは `KeyReceiver` にコールバックを登録して `start()` するだけで利用可能
- LCDアプリ以外（ログ収集、ショートカット処理、外部通知）でも同じ受信APIを再利用可能
- 低レイヤ制御が必要な場合のみ `GATTServer` を直接利用

### 利用サンプル（案）

```python
import asyncio

from raspi_receiver.lib import KeyReceiver, KeyType


async def main() -> None:
  receiver = KeyReceiver(device_name="RasPi-KeyAgent")

  def on_press(event) -> None:
    if event.key_type == KeyType.CHAR:
      print(f"CHAR: {event.value}")
    else:
      print(f"KEY: {event.key_type.value}:{event.value}")

  receiver.on_key_press = on_press
  receiver.on_disconnect = lambda ev: print("disconnected")

  await receiver.start()
  try:
    await asyncio.Event().wait()
  finally:
    await receiver.stop()


if __name__ == "__main__":
  asyncio.run(main())
```

### 公開API構成（案）

- `KeyReceiver`（高レベル）
  - BLE受信 + デシリアライズ + callback dispatch を提供
  - `on_key_press`, `on_key_release`, `on_connect`, `on_disconnect` を公開
  - `start()`, `stop()`, `is_connected`, `stats` を提供
- `GATTServer`（低レイヤ）
  - BLE GATTサーバーの起動/停止と `on_write(bytes)` のみを提供
  - 受信データ解釈は呼び出し側責務
